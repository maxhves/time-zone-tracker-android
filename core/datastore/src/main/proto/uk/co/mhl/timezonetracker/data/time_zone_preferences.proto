syntax = "proto3";

option java_package = "uk.co.mhl.timezonetracker.core.datastore";
option java_multiple_files = true;

message TimeZonePreferences {
  // Attempt to force uniqueness via map keys, since set does not exist.
  map<int32, bool> tracked_zone_ids = 1;
}